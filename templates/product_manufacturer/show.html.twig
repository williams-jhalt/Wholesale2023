{% extends 'base.html.twig' %}

{% block title %}ProductManufacturer{% endblock %}

{% block body %}
    <h1>ProductManufacturer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_manufacturer.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ product_manufacturer.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_manufacturer.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_product_manufacturer_index') }}">back to list</a>

    <a href="{{ path('app_product_manufacturer_edit', {'id': product_manufacturer.id}) }}">edit</a>

    {{ include('product_manufacturer/_delete_form.html.twig') }}
{% endblock %}
